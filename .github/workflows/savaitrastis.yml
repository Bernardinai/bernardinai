name: Savaitrascio Generatorius

on:
  schedule:
    - cron: '40 2 * * 6'
  workflow_dispatch:
    inputs:
      tikras_leidinys:
        description: 'Pazymeti, jei norite isleisti TIKRA numeri'
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  kurti-pdf:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Atsisiunciami repozitorijos failai
        uses: actions/checkout@v4

      - name: 2. Paruosiama Python aplinka
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 3. Diegiamos Linux sistemos bibliotekos
        run: |
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0

      - name: 4. Diegiamos Python bibliotekos
        run: |
          python -m pip install --upgrade pip
          pip install feedparser weasyprint tzdata

      - name: 5. Generuojamas savaitrastis
        env:
          MAILERLITE_API_KEY: ${{ secrets.MAILERLITE_API_KEY }}
          EVENT_NAME: ${{ github.event_name }}
          TIKRAS_LEIDINYS: ${{ github.event.inputs.tikras_leidinys }}
          WP_USERNAME: ${{ secrets.WP_USERNAME }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          WP_CATEGORY_ID: ${{ secrets.WP_CATEGORY_ID }}
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: python naujienlaiskis.py

      - name: 6. Issaugomas archyvas repozitorijoje
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add archyvas/ leidinio_numeris.txt
          if ! git diff --staged --quiet; then
            git commit -m "Automatinis PDF archyvo papildymas"
            git pull --rebase origin main
            git push
          fi

      - name: 7. Pranesimas apie SEKMINGA leidini
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.MAIL_SERVER }}
          server_port: ${{ secrets.MAIL_PORT || 465 }}
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "✅ Savaitraštis sėkmingai sugeneruotas ir paruoštas"
          to: ${{ secrets.NOTIFICATION_EMAIL || 'redakcija@bernardinai.lt' }}
          from: "Savaitraščio Botas <${{ secrets.MAIL_USERNAME }}>"
          body: |
            Savaitraštis sėkmingai sugeneruotas!
            
            - PDF failas sukurtas ir patalpintas į archyvą.
            - Įrašas WordPress svetainėje sukurtas.
            - MailerLite kampanija paruošta.
            - Paleidimo tipas: ${{ github.event.inputs.tikras_leidinys && 'Tikras numeris' || 'Bandomasis / Automatinis' }}

      - name: 8. Pranesimas ivyks KLAIDAI
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.MAIL_SERVER }}
          server_port: ${{ secrets.MAIL_PORT || 465 }}
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "❌ KLAIDA: Savaitraščio generavimas nepavyko"
          to: ${{ secrets.NOTIFICATION_EMAIL || 'redakcija@bernardinai.lt' }}
          from: "Savaitraščio Botas <${{ secrets.MAIL_USERNAME }}>"
          body: |
            Generuojant savaitraštį įvyko klaida!
            
            Patikrinkite veiksmų žurnalą GitHub Actions aplinkoje:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
